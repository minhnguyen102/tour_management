extends ../../layouts/default.pug

block main
    if(role.title)
        h1(class="mb-4") Quyền : #{role.title}

    if(role.description)
        div(class="mb-4") Mô tả : !{role.description}

    a(
        href=`${prefixAdmin}/roles/edit/${role.id}`
        class="btn btn-warning"
    ) Chỉnh sửa